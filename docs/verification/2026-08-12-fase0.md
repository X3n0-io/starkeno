# Evidenza Fase 0 — 12 agosto 2026

La tabella separa le verifiche locali concluse dai due gate che dipendono da sistemi
esterni. Nessun risultato remoto o live è simulato.

| Punto | Evidenza eseguita | Risultato | Commit principali | Stato / azione manuale |
|---|---|---|---|---|
| P1 — integrazione Codex | test plugin/hook/doctor; inventario cache | bundle e marketplace validi, plugin non ancora nella cache | `97e0a3e`, `9c20839` | **Aperto:** riavvio, `/plugins`, nuova sessione, `/hooks`, tre turni |
| P2 — recupero storico | doctor read-only, recupero esplicito, `quick_check`, hash sorgente | schema `0003` → `0005`, 1 riga preservata, 0 duplicati, sorgente invariata | `020b1bc`, `154f6b3` | **Chiuso localmente** |
| P3 — socket supervisore | test di fallimento `bind` e `listen`; suite strict | socket chiuso in entrambi i rami; 381 passati, 1 skip previsto | `eedcd29` | **Chiuso localmente** |
| P4 — packaging | build wheel/sdist e installazione wheel da directory estranea | CLI, report, risorse e migrazioni funzionanti dal wheel | `de68e04`, `fe6585a` | **Chiuso localmente** |
| P5 — portabilità | workflow validati; stress Windows locale | matrice definita su Windows/macOS/Linux e Python 3.12–3.14; stress 900/900 | `e8bf057` | **Aperto:** eseguire i job remoti dopo la creazione GitHub |
| P6 — supply chain | installazione vincolata, `pip check`, `pip-audit`, scanner segreti | requisiti coerenti, nessuna vulnerabilità nota, scanner pulito | `1bf5cef` | **Chiuso localmente** |
| P7 — snapshot pubblico | scanner privacy, `git archive`, build e suite strict nello snapshot | nessuna storia `.git`; build riuscita; 379 passati e 2 skip previsti nello snapshot | `dadf4b0`, `0bcb5c9`, `51a1092` | **Chiuso localmente; pubblicazione non autorizzata** |

## Baseline finale locale

- suite normale: 381 test passati, 1 skip previsto;
- suite con warning come errori: 381 test passati, 1 skip previsto;
- concorrenza: 900/900 scritture, zero giri supervisore falliti, zero risposte HTTP
  non-200, `journal_mode=wal`, integrità `ok`;
- wheel e sdist costruiti;
- `pip check`: nessun requisito rotto;
- `pip-audit`: nessuna vulnerabilità nota;
- scanner privacy e segreti: nessun rilievo;
- recupero reale: schema `0005`, una riga preservata, zero duplicati, sorgente invariata.

I job da configurare come status check obbligatori dopo la pubblicazione sono `tests`,
`package`, `audit` e `stress`.
